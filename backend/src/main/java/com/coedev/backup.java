package com.coedev;

public class backup {
    /*     @GetMapping("/greet")
     public GreetResponse greet(@RequestParam(value = "name", required = false) String name){
        String greetMessage = name == null || name.isBlank() ? "Hello" : "Hello " + name;
         return new GreetResponse(greetMessage, List.of("java", "Golang"), new Person("Alex", 28, 30000));
     }

     record Person(String name, int age, double income){}
     record GreetResponse(String greet, List<String> fav, Person person){} */
}
